# Шаблон секретов. Скопируй в secrets.yaml и заполни значениями. secrets.yaml в .gitignore.
---
apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: spark-jobs
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "minioadmin"
  AWS_SECRET_ACCESS_KEY: "CHANGE_ME"
  AWS_ENDPOINT_URL: "http://minio.storage.svc.cluster.local:9000"
---
apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: data-platform
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "minioadmin"
  AWS_SECRET_ACCESS_KEY: "CHANGE_ME"
  AWS_ENDPOINT_URL: "http://minio.storage.svc.cluster.local:9000"
---
apiVersion: v1
kind: Secret
metadata:
  name: kafka-credentials
  namespace: spark-jobs
type: Opaque
stringData:
  KAFKA_BOOTSTRAP_SERVERS: "kafka.ijklmn.xyz:9092"
