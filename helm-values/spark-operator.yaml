spark:
  jobNamespaces:
    - spark-jobs

webhook:
  enable: true
  port: 9443

controller:
  workers: 10

metrics:
  enable: true
  port: 8080
  portName: metrics
  endpoint: /metrics

resources:
  requests:
    cpu: 100m
    memory: 384Mi
  limits:
    cpu: "1"
    memory: 1024Mi

