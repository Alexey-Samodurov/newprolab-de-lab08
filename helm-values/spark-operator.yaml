spark:
  jobNamespaces:
    - spark-jobs

webhook:
  enable: true
  port: 9443

controller:
  workers: 5

metrics:
  enable: true
  port: 8080
  portName: metrics
  endpoint: /metrics

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

