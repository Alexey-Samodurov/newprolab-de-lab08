spark:
  jobNamespaces:
    - spark-jobs

webhook:
  enable: true
  port: 9443

controller:
  workers: 10

metrics:
  enable: true
  port: 8080
  portName: metrics
  endpoint: /metrics

resources:
  requests:
    cpu: 500m
    memory: 768Mi
  limits:
    cpu: "1"
    memory: 1536Mi

